{
  "name": "connpass-advanced-stats",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "popup"
    ],
    "nohoist": [
      "**"
    ]
  },
  "scripts": {
    "dev": "concurrently -k \"yarn workspace popup dev\" ",
    "lint": "concurrently -k \"yarn workspace popup lint\" ",
    "test": "concurrently -k \"yarn workspace popup test\" ",
    "build": "concurrently -k \"yarn workspace popup build\" "
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "eslint": "^8.32.0",
    "prettier": "^2.8.4",
    "typescript": "^4.9.4"
  }
}